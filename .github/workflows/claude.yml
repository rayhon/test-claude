name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      issues: read
      id-token: write
      actions: read  # lets Claude read CI results on PRs
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code (via Anthropic-compatible gateway)
        uses: anthropics/claude-code-action@v1
        env:
          # Point the CLI at your proxy/gateway
          ANTHROPIC_BASE_URL: https://api.z.ai/api/anthropic
          # Provide the token in the name the action expects
          ANTHROPIC_API_KEY: ${{ secrets.GLM_API_KEY }}
        with:
          # Do NOT set `anthropic_api_key:` input, we’re supplying it via env instead.
          # anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}

          # Let the action read CI results (optional but useful)
          additional_permissions: |
            actions: read

          # If your proxy requires a specific model id, you can pass it here:
          # claude_args: '--model glm-4'
